nname: tirzetrack
description: "Aplicativo para acompanhamento de tratamento com GLP-1"
publish_to: 'none'

version: 1.0.38+39

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2
  firebase_core: ^3.1.0
  firebase_auth: ^5.1.0
  shared_preferences: ^2.2.3
  google_generative_ai: ^0.4.0 # <--- ADICIONE ESTA LINHA

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
